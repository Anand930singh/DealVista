spring.application.name=CouponCollectorBackend

# API served under /api so that full paths are /api/coupons, /api/auth, etc.
server.servlet.context-path=/api

# PostgreSQL database configuration
spring.datasource.url=jdbc:postgresql://ep-lively-fog-a58gh9b4-pooler.us-east-2.aws.neon.tech/couponCollector?sslmode=require&channel_binding=require
spring.datasource.username=anand2002ksingh
spring.datasource.password=1PZiCSGWlEw7
spring.datasource.driver-class-name=org.postgresql.Driver

# Gemini Credentials
gemini.api_key=AIzaSyCI6qJmK87hAKdRHVmmNYbVg1y7lvN74Kw

# JPA SETTINGS
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# JWT Configuration
jwt.secret=your-secret-key-should-be-at-least-256-bits-long-for-hs256-algorithm-security
jwt.expiration=86400000

# CORS: only these origins are allowed (comma-separated). No wildcards.
cors.allowed-origins=https://coupon-collector.vercel.app,http://localhost:5173,http://localhost:3000,http://localhost:5174,http://127.0.0.1:5173,http://127.0.0.1:3000