# Reward Points Flow - Visual Guide & Code Reference\n\n## 🎯 Quick Visual Overview\n\n### Scenario 1: User Lists Coupon\n```\n┌─────────────────────────────────────────────────────┐\n│  User fills coupon form and clicks \"List Coupon\"   │\n└────────────────┬────────────────────────────────────┘\n                 │\n                 ▼\n┌─────────────────────────────────────────────────────┐\n│  Frontend validates form                            │\n└────────────────┬────────────────────────────────────┘\n                 │\n                 ▼\n┌─────────────────────────────────────────────────────┐\n│  POST /coupons (with coupon data)                   │\n└────────────────┬────────────────────────────────────┘\n                 │\n                 ▼\n┌─────────────────────────────────────────────────────┐\n│  Backend: CouponListingService.save()               │\n│    ✓ Creates coupon                                 │\n│    ✓ Calls addPointsByEmail(email, 5)              │\n└────────────────┬────────────────────────────────────┘\n                 │\n                 ▼\n┌─────────────────────────────────────────────────────┐\n│  Database: UserDetail.points += 5                   │\n└────────────────┬────────────────────────────────────┘\n                 │\n                 ▼\n┌─────────────────────────────────────────────────────┐\n│  Response: 201 CREATED with coupon data             │\n└────────────────┬────────────────────────────────────┘\n                 │\n                 ▼\n┌─────────────────────────────────────────────────────┐\n│  Frontend: showToast(\"Coupon listed successfully\",  │\n│                     \"success\")                      │\n│                                                     │\n│  🟢 GREEN TOAST APPEARS (top-right corner)          │\n│     \"Coupon listed successfully!\"                   │\n│                                                     │\n│  ✓ User sees: Points increased by 5                 │\n└─────────────────────────────────────────────────────┘\n```\n\n---\n\n### Scenario 2: User Views Coupon Code\n```\n┌─────────────────────────────────────────────────────┐\n│  User opens coupon details modal                    │\n│                                                     │\n│  ✨ Coupon Code                                     │\n│  ████████████ [👁] [📋]                             │\n│                                                     │\n│  User clicks Eye icon                               │\n└────────────────┬────────────────────────────────────┘\n                 │\n                 ▼\n┌─────────────────────────────────────────────────────┐\n│  Frontend: handleRevealCode()                       │\n│    ✓ Checks if code already visible                 │\n│    ✓ If new reveal: sets isDeductingPoints = true   │\n│    ✓ Button becomes disabled with \"...\"             │\n└────────────────┬────────────────────────────────────┘\n                 │\n                 ▼\n┌─────────────────────────────────────────────────────┐\n│  POST /coupons/{id}/view-code                       │\n└────────────────┬────────────────────────────────────┘\n                 │\n                 ▼\n        ┌────────┴────────┐\n        │                 │\n   SUCCESS            INSUFFICIENT POINTS\n        │                 │\n        ▼                 ▼\n┌──────────────────┐ ┌─────────────────────┐\n│ Database update: │ │ Check failed:       │\n│ points -= 5      │ │ currentPoints < 5   │\n└────────┬─────────┘ └────────┬────────────┘\n         │                    │\n         ▼                    ▼\n┌──────────────────┐ ┌─────────────────────┐\n│ Response 200:    │ │ Response 400:       │\n│ {               │ │ {                   │\n│  message:       │ │  message:           │\n│  \"5 points...\" │ │  \"Insufficient...\"  │\n│ }               │ │ }                   │\n└────────┬─────────┘ └────────┬────────────┘\n         │                    │\n         ▼                    ▼\n┌──────────────────┐ ┌─────────────────────┐\n│ showCode = true  │ │ showCode stays false │\n│ 🔓 Code visible  │ │ 🔒 Code hidden      │\n└────────┬─────────┘ └────────┬────────────┘\n         │                    │\n         ▼                    ▼\n┌──────────────────┐ ┌─────────────────────┐\n│ 🔵 BLUE TOAST    │ │ 🔴 RED TOAST        │\n│ \"5 points        │ │ \"Failed to reveal   │\n│  deducted!       │ │  code. You may      │\n│  Code revealed.\" │ │  not have enough    │\n│                  │ │  points.\"           │\n│ ✓ Points -= 5    │ │ ✗ Points unchanged  │\n└──────────────────┘ └─────────────────────┘\n```\n\n---\n\n## 💻 Code Reference\n\n### Backend Implementation\n\n#### 1. Add Points Method (RewardPointsService.java)\n```java\npublic void addPointsByEmail(String email, int pointsToAdd) {\n    UserDetail user = userDetailRepository.findByEmail(email)\n            .orElseThrow(() -> new RuntimeException(\"User not found\"));\n    \n    int currentPoints = user.getPoints() != null ? user.getPoints() : 0;\n    user.setPoints(currentPoints + pointsToAdd);\n    userDetailRepository.save(user);\n}\n```\n\n#### 2. Deduct Points Method (RewardPointsService.java)\n```java\npublic void deductPointsByEmail(String email, int pointsToDeduct) {\n    UserDetail user = userDetailRepository.findByEmail(email)\n            .orElseThrow(() -> new RuntimeException(\"User not found\"));\n    \n    int currentPoints = user.getPoints() != null ? user.getPoints() : 0;\n    if (currentPoints < pointsToDeduct) {\n        throw new RuntimeException(\"Insufficient reward points\");\n    }\n    user.setPoints(currentPoints - pointsToDeduct);\n    userDetailRepository.save(user);\n}\n```\n\n#### 3. View Code Endpoint (CouponController.java)\n```java\n@PostMapping(\"/{id}/view-code\")\npublic ResponseEntity<?> viewCouponCode(@PathVariable UUID id, \n                                        Authentication authentication) {\n    try {\n        rewardPointsService.deductPointsByEmail(authentication.getName(), 5);\n        Map<String, String> response = new HashMap<>();\n        response.put(\"message\", \"5 points deducted successfully\");\n        return ResponseEntity.ok(response);\n    } catch (RuntimeException e) {\n        Map<String, String> error = new HashMap<>();\n        error.put(\"message\", \n            e.getMessage() != null ? e.getMessage() : \"Failed to deduct points\");\n        return ResponseEntity.status(HttpStatus.BAD_REQUEST).body(error);\n    }\n}\n```\n\n---\n\n### Frontend Implementation\n\n#### 1. API Call (api.js)\n```javascript\nviewCouponCode: async (couponId) => {\n    return apiRequest(`/coupons/${couponId}/view-code`, {\n        method: \"POST\",\n    })\n}\n```\n\n#### 2. Handle Reveal Code (CouponDetailModal.jsx)\n```javascript\nconst handleRevealCode = async () => {\n    if (showCode) {\n        // User is hiding the code, just toggle\n        setShowCode(false)\n        return\n    }\n\n    // User is revealing the code - deduct 5 points\n    setIsDeductingPoints(true)\n    try {\n        await couponAPI.viewCouponCode(coupon.id)\n        setShowCode(true)\n        if (onShowToast) {\n            onShowToast(\"5 points deducted! Coupon code revealed.\", \"info\")\n        }\n    } catch (error) {\n        if (onShowToast) {\n            onShowToast(\n                error.message || \"Failed to reveal code. You may not have enough points.\",\n                \"error\"\n            )\n        }\n    } finally {\n        setIsDeductingPoints(false)\n    }\n}\n```\n\n#### 3. Eye Button with Loading State\n```jsx\n<button\n    className=\"btn-reveal\"\n    onClick={handleRevealCode}\n    disabled={isDeductingPoints}\n    title={showCode ? \"Hide code\" : \"Reveal code (costs 5 points)\"}\n>\n    {isDeductingPoints ? (\n        <span>...</span>\n    ) : showCode ? (\n        <EyeOff size={18} />\n    ) : (\n        <Eye size={18} />\n    )}\n</button>\n```\n\n---\n\n## 📊 State Diagram\n\n### Frontend Component State\n```\n┌──────────────────────────────────────────┐\n│  CouponDetailModal State                 │\n│                                          │\n│  ┌────────────────────────────────────┐ │\n│  │ showCode: boolean                  │ │\n│  │  • false = code hidden             │ │\n│  │  • true = code visible             │ │\n│  └────────────────────────────────────┘ │\n│                                          │\n│  ┌────────────────────────────────────┐ │\n│  │ isDeductingPoints: boolean         │ │\n│  │  • false = idle                    │ │\n│  │  • true = processing API call      │ │\n│  └────────────────────────────────────┘ │\n│                                          │\n│  ┌────────────────────────────────────┐ │\n│  │ copied: boolean                    │ │\n│  │  • false = normal                  │ │\n│  │  • true = \"Copied!\" shown (2sec)  │ │\n│  └────────────────────────────────────┘ │\n└──────────────────────────────────────────┘\n```\n\n---\n\n## 🎨 Toast Notifications\n\n### Success Toast\n```\n┌─────────────────────────────────────┐\n│ 🟢 ✓ Coupon listed successfully!    │\n└─────────────────────────────────────┘\nColor: Green (#4caf50)\nDuration: 3-4 seconds\nPosition: Top-right corner\n```\n\n### Error Toast\n```\n┌──────────────────────────────────────────────────┐\n│ 🔴 ✗ Failed to reveal code. You may not have    │\n│      enough points.                              │\n└──────────────────────────────────────────────────┘\nColor: Red\nDuration: 3-4 seconds\nPosition: Top-right corner\n```\n\n### Info Toast\n```\n┌──────────────────────────────────────────────────┐\n│ 🔵 ℹ 5 points deducted! Coupon code revealed.  │\n└──────────────────────────────────────────────────┘\nColor: Blue (#2196f3)\nDuration: 3-4 seconds\nPosition: Top-right corner\n```\n\n---\n\n## 🔄 Data Flow Summary\n\n### Points Addition\n```\n1. User → Frontend Form\n2. Frontend → Backend POST /coupons\n3. Backend CouponListingService.save()\n4. CouponListingService → RewardPointsService.addPointsByEmail()\n5. RewardPointsService → Database (UPDATE UserDetail SET points = points + 5)\n6. Response → Frontend\n7. Frontend → Toast (GREEN) + Points Display Update\n```\n\n### Points Deduction\n```\n1. User → Frontend Eye Button Click\n2. Frontend → Backend POST /coupons/{id}/view-code\n3. Backend CouponController.viewCouponCode()\n4. CouponController → RewardPointsService.deductPointsByEmail()\n5. RewardPointsService → Validate (points >= 5)\n6. If Valid: Database (UPDATE UserDetail SET points = points - 5)\n   If Invalid: Throw Exception\n7. Response → Frontend\n8. Frontend → Toast + State Update\n   - Success: Show code + BLUE toast\n   - Error: Hide code + RED toast\n```\n\n---\n\n## ✅ HTTP Status Codes\n\n| Operation | Success | Failure |\n|-----------|---------|----------|\n| List Coupon | 201 CREATED | 400 BAD_REQUEST |\n| View Code | 200 OK | 400 BAD_REQUEST |\n| Get Coupons | 200 OK | 400 BAD_REQUEST |\n\n---\n\n## 🔐 Security Flow\n\n```\n┌─────────────────────────────────┐\n│ User Login                      │\n│ → Gets JWT Token                │\n│ → Token stored in localStorage  │\n└────────────┬────────────────────┘\n             │\n             ▼\n┌─────────────────────────────────┐\n│ All API Requests                │\n│ → Token in Authorization header │\n│ → \"Bearer {token}\"              │\n└────────────┬────────────────────┘\n             │\n             ▼\n┌─────────────────────────────────┐\n│ Backend                         │\n│ → Extracts user from JWT        │\n│ → Gets email from authentication│\n│ → Performs operation for user   │\n└─────────────────────────────────┘\n```\n\n---\n\n## 📱 UI Component Hierarchy\n\n```\nBrowseCoupons\n├── Navbar (shows points)\n├── CouponGrid\n│   └── CouponCard (click to open)\n│       └── onClick → setSelectedCoupon()\n├── CouponDetailModal\n│   ├── Modal Content\n│   ├── Eye Button\n│   │   └── onClick → handleRevealCode()\n│   │       └── couponAPI.viewCouponCode()\n│   │           └── onShowToast(message, type)\n│   └── Toast (from parent's useToast)\n└── ToastContainer\n    └── Displays: success/error/info/warning\n```\n\n---\n\n*Visual Guide v1.0 - 2026-02-14*\n